true: safe_string, bin_annot, debug
<*/*>: warn(@32@20@25@26@34@33@37@39)

<lib>: include

# library
<lib/*.ml>: use_bigarray, use_ppx_debug(false)

# ppx
<ppx/*.{ml,mli,byte,native}>: package(compiler-libs.common)

# test
<lib_test/*.{ml,mli,byte,native}>: package(re.str), package(camlzip), package(alcotest), use_bigarray

# binary
<bin/dpipe.{ml,mli,byte,native}>: use_unix, use_bigarray, linkdep(bin/bs.o)
<bin/dcpr.{ml,mli,byte,native}>: use_bigarray, package(camlzip), package(cmdliner)
